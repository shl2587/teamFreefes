<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC 
	"-//mybatis.org//DTD Mapper 3.0//EN" 
	"https://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.ohmija.repository.PostMessageDAO">

	<insert id="sendMessage" parameterType="message">
		insert into post_message (writer, receiver, title, content, category, result)
			values 
				(#{writer}, #{receiver}, #{title}, #{content}, #{category}, #{result})
	</insert>
	
</mapper>

