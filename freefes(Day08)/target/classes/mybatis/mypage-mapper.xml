<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC 
	"-//mybatis.org//DTD Mapper 3.0//EN" 
	"https://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.ohmija.repository.MypageDAO">

<update id="changepw" parameterType="String"> 
	update member set userpw = #{userpw2} where userid = #{loggedInUserId}
</update>

<select id="selectuserpw" parameterType="string" resultType="int">
	select count(*) from member
			where
				userpw = #{userpw}
</select>

<update id="userpwNew" parameterType="member"> 
	update member set userpw = #{userpwNew} where userid = #{userid}
</update>

<update id="update" parameterType="member"> 
	update member set 
	USERPW = #{USERPW} 
, PROFILE_IMG = #{PROFILE_IMG}
,  NICKNAME = #{NICKNAME}	
, ADDRESS = #{ADDRESS}	
, PHONENUMBER = #{PHONENUMBER} 
where idx=#{idx}
</update>

</mapper>















